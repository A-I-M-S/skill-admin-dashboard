<h1>Chatbot · Handoff queue</h1>
<p class="muted">
  Customers currently in <code>handoff</code> flow. Each row links to the per-phone
  conversation and to the admin-send form pre-filled with that phone.
</p>

<%
  function stateBadge(state) {
    if (state === 'ok') return '<span class="badge ok">ok</span>';
    if (state === 'chatbot_admin_unavailable') return '<span class="badge warn">wrapper not installed</span>';
    if (state === 'invalid_json') return '<span class="badge warn">invalid json</span>';
    if (state === 'spawn_failed') return '<span class="badge warn">spawn failed</span>';
    if (state === 'bridge_unreachable') return '<span class="badge warn">bridge unreachable</span>';
    return '<span class="badge muted">' + state + '</span>';
  }
%>

<div class="meta">
  <p>
    State: <%- stateBadge(view.state) %>
    · binary: <code><%= view.binPath %></code>
  </p>
  <% if (view.error) { %>
    <p class="muted"><%= view.error %></p>
  <% } %>
</div>

<% if (view.state === 'chatbot_admin_unavailable') { %>
  <div class="card warn">
    <h3>skill-chatbot admin wrapper not installed</h3>
    <p>The <code>bin/chatbot-admin</code> wrapper is not installed at <code><%= view.binPath %></code>.
       This page will return 503 with <code>Retry-After: 60</code> until the wrapper ships.</p>
  </div>
<% } else if (view.state === 'ok' && view.queue.length === 0) { %>
  <div class="flash">No handoffs currently open. Good day.</div>
<% } else if (view.state === 'ok') { %>
  <table class="handoff">
    <thead>
      <tr>
        <th>phone</th>
        <th>reason</th>
        <th>summary</th>
        <th>since</th>
        <th>last message</th>
        <th>language</th>
        <th>actions</th>
      </tr>
    </thead>
    <tbody>
      <% for (const h of view.queue) { %>
        <tr>
          <td><code><%= h.phone %></code></td>
          <td><%= h.reason %></td>
          <td><%= h.summary %></td>
          <td><%= h.since %></td>
          <td><%= h.lastMessageAt ? h.lastMessageAt : '\u2014' %></td>
          <td><%= h.language ? h.language : '\u2014' %></td>
          <td class="actions">
            <a class="action-link"
               href="/chatbot/conversations/<%= encodeURIComponent(h.phone) %>">view msgs</a>
            <a class="action-link"
               href="/chatbot/send?to=<%= encodeURIComponent(h.phone) %>">admin send</a>
            <% if (h.isFallback) { %>
              <span class="badge warn">fallback</span>
            <% } %>
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
<% } %>

<style>
  .meta { display: flex; gap: 1rem; align-items: center; flex-wrap: wrap; }
  table.handoff { border-collapse: collapse; width: 100%; }
  table.handoff th, table.handoff td { border-bottom: 1px solid #e1e6e9; padding: 0.4rem 0.6rem; vertical-align: top; text-align: left; }
  table.handoff th { background: #f4f7f9; font-size: 0.85rem; }
  table.handoff td.actions { display: flex; gap: 0.4rem; flex-wrap: wrap; align-items: center; }
  table.handoff .action-link { font-size: 0.85rem; }
  .muted { color: #6a7d83; font-size: 0.85rem; margin: 0.2rem 0 0.6rem 0; }
  .badge { display: inline-block; padding: 0 0.4rem; border-radius: 3px; font-size: 0.75rem; vertical-align: middle; }
  .badge.ok { background: #d4edda; color: #155724; }
  .badge.muted { background: #e9ecef; color: #495057; }
  .badge.warn { background: #fff3cd; color: #856404; }
  .card.warn { background: #fff3cd; border: 1px solid #ffeeba; padding: 0.8rem 1rem; border-radius: 4px; margin: 0.6rem 0; }
  .card.warn h3 { margin: 0 0 0.4rem 0; font-size: 1rem; }
  .card.warn p { margin: 0.2rem 0; }
</style>